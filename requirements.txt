# Flet is pinned to an exact version. See pyproject.toml for the upgrade policy
# (breaking enum/API changes between minor releases, flet_compat.py shim).
flet==0.28.3
flet-desktop==0.28.3
supabase>=2.0.0,<2.5.0
bcrypt>=4.0.0
httpx>=0.25.0,<0.28.0
packaging>=21.0
